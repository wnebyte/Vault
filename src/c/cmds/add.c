#include <stdio.h>
#include "cmds/add.h"
#include "vault.h"
#include "types.h"

int add_exe(int argc, char *argv[]) {
    fprintf(stdout, "add\n");
    if (argc == 1) {

    }
    return EXIT_SUCCESS;
}
